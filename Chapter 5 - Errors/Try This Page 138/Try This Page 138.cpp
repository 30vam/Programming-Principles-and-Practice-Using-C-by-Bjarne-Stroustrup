// Try This Page 138.cpp : Try to compile those examples and see how the compiler responds.
//

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int area(int length, int width); // calculate area of a rectangle

int main()
{
    int s1 = area(7; // error: ) missing
    int s2 = area(7) // error: ; missing
    Int s3 = area(7); // error: Int is not a type
    int s4 = area('7); // error: non-terminated character ( ' missing)
}

